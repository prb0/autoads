{% extends 'layout.html.twig' %}

{% block main %}
<div class="row mt-3">
	<div class="col-12 mt-5">
		<h4>Редактировать объявление</h4>
		{{ form_start(formAd) }}
		{{ form_widget(formAd) }}
		{{ form_end(formAd) }}
	</div>	
</div>
{% endblock %}
